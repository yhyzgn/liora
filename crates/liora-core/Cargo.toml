[package]
name = "liora-core"
version = "0.1.0"
edition = "2024"
description = "Core Liora app configuration, theme initialization, portals, and shared native GPUI helpers."
license = "LicenseRef-Liora"
repository = "https://github.com/yhyzgn/liora"
publish = false

[dependencies]
gpui.workspace = true
liora-theme = { path = "../liora-theme" }
