# Combobox

`Combobox` 是可搜索选择器：当选项数量较多、需要分组、需要多选、或需要在下拉面板中放置创建入口时，用它替代普通 `Select`。固定少量选项仍建议使用 `Select`。

## 单选搜索

### 效果

::LioraDemo{component="ComboboxBasic"}::

### 代码

```rust src="combobox/basic.rs"
```

## 分组与禁用项

### 效果

::LioraDemo{component="ComboboxGrouped"}::

### 代码

```rust src="combobox/grouped.rs"
```

## 多选

### 效果

::LioraDemo{component="ComboboxMultiple"}::

### 代码

```rust src="combobox/multiple.rs"
```

## Footer 扩展

### 效果

::LioraDemo{component="ComboboxFooter"}::

### 代码

```rust src="combobox/footer.rs"
```
