# Splitter

`Splitter` 用于在同一容器内展示左右两个面板，适合编辑器、详情页和主从视图。

## 基础用法

左右面板分别通过 `left` 和 `right` 注入任意原生节点，`bordered` 可添加外边框。

### 效果

::Demo{component="SplitterBasic"}::

### 代码

```rust src="splitter/basic.rs"
```
